<%= provide(:title, "投稿詳細の作成") %>

<div class="field my-5 mx-auto">
  <p><%= l @frame.updated_at, format: :date %></p>
  <p>statement</p><br />
  <p><%= @frame.statement %></p>
</div>

<%# Ajax化でモーダル式のTag_newフォーム表示 %>
<%# tags#createでframe_idを使うので、frameをtags#newにも渡す。 %>
<%= link_to new_tag_path(frame: @frame), remote: true, class: "btn btn-lg, btn-primary" do %>
<i class="fas fa-plus"></i>
<% end%>

<div class="actions my-6 mx-auto">
  <%#= f.submit "投稿詳細の作成へ", class:"block mx-auto" %>
</div>

<%# Tag_newのモーダルがここに表示される %>
<div id="tag_model"></div>